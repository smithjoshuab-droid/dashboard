name: Refresh Dashboard

on:
  schedule:
    # 9:00 AM Central Standard Time = 15:00 UTC
    # (During Daylight Saving Time this runs at 10 AM CT — adjust to 14:00 if preferred)
    - cron: '0 15 * * 1-5'
  workflow_dispatch:   # Adds a "Run workflow" button so you can trigger it manually anytime

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install openpyxl

      - name: Download spreadsheet from SharePoint
        env:
          SHAREPOINT_URL: ${{ secrets.SHAREPOINT_FILE_URL }}
          SHAREPOINT_USER: ${{ secrets.SHAREPOINT_USER }}
          SHAREPOINT_PASS: ${{ secrets.SHAREPOINT_PASS }}
        run: python download_spreadsheet.py

      - name: Rebuild dashboard
        run: python build_dashboard.py

      - name: Commit updated dashboard
        run: |
          git config user.name "Dashboard Bot"
          git config user.email "dashboard-bot@users.noreply.github.com"
          git add index.html
          git diff --staged --quiet || git commit -m "Dashboard refresh $(date +'%Y-%m-%d %H:%M')"
          git push
